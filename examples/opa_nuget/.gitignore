# Build artifacts — regenerated by build.sh (needs ../../bin/goclr + runtime DLLs).
# host/go.mod and host/go.sum ARE committed (pin opa v1.18.2 + deps).
bin/
obj/
*.dll
*.nupkg

# host/vendor is regenerated by build.sh (go mod vendor); overlays under
# host/goclr.overlays ARE committed.
host/vendor/
host/host.runtimeconfig.json
